
1. **Aliases** – Using `DT_ALIAS` to rename or create convenient references for hardware components.

2. **Status Management** – Using `status = "disabled";` to deactivate a node in the device tree.

3. **DT_PROP** – Reading a property from a node using `DT_PROP(node, label)`.

4. **Overlay File** – Customizing board configuration through the `app.overlay` file.
